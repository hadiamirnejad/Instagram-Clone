import {atom} from 'recoil'

export const modalAddPostState = atom({
  key: 'modalAddPostState',
  default: false,
})

export const modalSignInState = atom({
  key: 'modalSignInState',
  default: false,
})